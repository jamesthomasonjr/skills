name: review-dump-letters

on:
  push:
  pull_request:

jobs:
  score:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Score recorded review dump letters
        run: ./scripts/test-score-review-dump-letters.sh
      - name: Score recorded review-audit dump letters
        run: ./scripts/test-score-review-audit-dump-letters.sh
